#include "main.h"
/**
 * swap_int - swaps the value of two integers
 *0a: pointer to be updated 
 *0b: also pointer to be updated
 * Return; The value of the swap
 */
void swap_int(int *a, int *b)
{
	int y;
	y = *a
	*a = *b;
	*b = y;
}
